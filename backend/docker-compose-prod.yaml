name: backend-prod
services:
  backend-service:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: backend-service
    env_file:
      - .env.prod
    ports:
      - '${BACKEND_PORT}:${BACKEND_PORT}'
    networks:
      - backend-network
    restart: on-failure